# Target for the TI BeaglePlay Board (https://www.beagleboard.org/boards/beagleplay)
version: '2'
mpp-vars:
  k3_board: 'am62x_beagleplay_gp'

# To derive and override mpp vars we move the overrides to an include file
pipelines:
  - mpp-import-pipelines:
      path: include/k3.ipp.yml
