name: install_rpms
content:
  rpms:
    - strace
    - coreutils-debuginfo
    - htop
  enable_repos:
    - debug
  repos:
    - id: epel
      baseurl: https://mirrors.hostiserver.com/fedora/epel/10/Everything/$arch/

qm:
  content:
    rpms:
      - less
      - rpm-debuginfo
      - readline7
    enable_repos:
      - debug
    repos:
      - id: epel
        baseurl: https://mirrors.hostiserver.com/fedora/epel/10/Everything/$arch/
